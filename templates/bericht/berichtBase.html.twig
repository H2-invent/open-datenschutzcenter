<!DOCTYPE html>
<style>
    /** Define the margins of your page **/
    @page {
        margin: 90px 60px;
    }

    header {
        position: fixed;
        top: -50px;
        left: 0px;
        right: 0px;
        height: 40px;

        /** Extra personal styles **/
        text-align: center;
        line-height: 20px;
    }

    footer {
        position: fixed;
        bottom: -50px;
        left: 0px;
        right: 0px;
        height: 30px;

        /** Extra personal styles **/
        text-align: center;
        line-height: 20px;
    }
</style>
<body>
<header>
    {{ daten[0].nummer }} - {{ team.name }}
</header>

<main>
    <div style="font-family: Helvetica">
        <h1 style="margin-top: 200px">{{ titel }}</h1>
        <h3 style="margin-top: 50px">Verantwortliche Stelle: {{ team.name }}</h3>
        <h4>{{ team.signature|nl2br }}</h4>
        <h5 style="margin-top: 40px">Export erstellt am: {{ "now"|date('d.m.Y H:i') }}</h5>
        <h5>Version erstellt von: {{ app.user.email }}</h5>

        {% block body %}{% endblock %}
    </div>
</main>

<footer>
    Erstellt am: {{ "now"|date("d.m.Y H:i") }} mit <strong>Open-Datenschutz Center</strong><br>
    <small>Open-Datenschutz Center ist die Open Source Lösung um Datenschutzdokumente schnell, einfach und sicher zu
        erstellen</small>
</footer>
</body>
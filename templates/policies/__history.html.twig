{% trans_default_domain 'policies' %}
<a href="{{ path('policy_edit',{'id':p.id}) }}"><span>{{ p.createdAt|date('d.m.Y') }} -- {{ p.statusString }} -- {{ p.user.email }}  {% trans %}{{ p.approved?'versionApproved' }}{% endtrans %} </span></a>
{% if p.previous %}
    {{ include('policies/__history.html.twig', {'p':p.previous}) }}
{% endif %}

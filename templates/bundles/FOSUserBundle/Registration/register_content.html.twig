{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div>
        <button class="btn btn-primary btn-raised btn-block" type="submit" value="{{ 'registration.submit'|trans }}" >{{ 'registration.submit'|trans }}</button>
    </div>
{{ form_end(form) }}

<!DOCTYPE html>
<html>
    {{ include('base/__head.html.twig') }}
    <body>
        <main>
            {% block back %}{% endblock %}
            <div id="content" class="container mx-auto max-w-2xl">
                {% block content %}{% endblock %}
            </div>
        </main>
    </body>
</html>
<a href="{{ path('audit_tom_edit',{'tom':p.id}) }}"><span>{{ p.createdAt|date('d.m.Y H:i') }} -- {{ p.titel }}-- {{ p.user.email }}</span></a>
{% if p.previous %}
    {{ include('audit_tom/__history.html.twig', {'p':p.previous}) }}
{% endif %}
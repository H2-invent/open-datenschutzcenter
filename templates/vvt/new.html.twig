{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="h2-responsive">{{ title }} {% if not activNummer %}<span class="small"> id: {{ vvt.nummer }}{% endif %}</span></h2>
    {{ include('vvt/__form.html.twig') }}
    <p>Erstellt von: {{ vvt.user.email }}</p>
    {% if vvt.previous %}
        <div class="card card-body mt-2">
            <h2>Historie</h2>
            {{ include('vvt/__history.html.twig', {'p':vvt.previous}) }}
        </div>
    {% endif %}
{% endblock %}

{% block javascript %}
{% endblock %}

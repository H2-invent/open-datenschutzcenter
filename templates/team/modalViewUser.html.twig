<div>
    <div>

        <div>
            <h5>{{ title }}</h5>

        </div>
        <div>
            {{ form_start(form, {'attr': {'action': path('team_mitglieder_create', {'type':type, 'id':teamId}), 'method': 'POST'}}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>

{% extends 'team/index.html.twig' %}

{% block body %}
    <div class="container">
        <h2 class="h2-responsive">{{ title }}</h2>

        <div class="card card-body">

            {% if data %}
                <div class="row">
                    <div class="col-md-6">
                        {{ 'email'|trans }}: {{ data.email }}<br>
                        {{ 'userName'|trans }}: {{ data.username }}
                    </div>
                    <div class="col-md-6">
                        <a href="{{ path('team_dsb_remove', {'id':data.id}) }}" class="btn btn-sm btn-danger"
                           onClick="return confirm('{% trans %}confirmRemoveDsb{% endtrans %}')">
                                {% trans %}remove{% endtrans %}
                        </a>
                    </div>
                </div>
            {% else %}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
                <p class="alert-danger mt-5 text-center">
                    {% trans %}addExternalDsbInfo{% endtrans %}
                </p>
            {% endif %}


        </div>
    </div>
    {{ include('base/__snack.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block CTA %}
    <a class="btn btn-primary ml-1 btn-sm" href="{{ path('task_new') }}">
        <span>Neue Aufgabe anlegen</span>
    </a>
{% endblock %}

{% block body %}
    <h2 class="h2-responsive">Aufgaben Übersicht</h2>
    <div class="card card-body">
        <div class="table-responsive">
            <table id="data-table" class="table display table-striped">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Titel</th>
                    <th scope="col">Aufgabe</th>
                    <th scope="col">Titel</th>
                    <th scope="col">Beschreibung</th>
                    <th scope="col">Erledigt</th>
                </tr>
                </thead>
                <tbody>
                {% for t in task %}
                    <tr class="clickable-row" data-href='{{ path('task_edit',{'id':t.id}) }}'>
                        <th scope="row">{{ t.id }}</th>
                        <td>{{ t.title }}</td>
                        <td>{{ t.task|raw }}</td>
                        <td>{{ t.createdAt|date('d.m.Y') }}</td>
                        <td>{{ t.done ?'Ja':'Nein' }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

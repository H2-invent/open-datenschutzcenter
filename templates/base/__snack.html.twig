{% for flashMessage in app.session.flashbag.get('success') %}
    {{ include ('base/__snackItem.html.twig', {snackClass: 'success'}) }}
{% endfor %}

{% for flashMessage in app.session.flashbag.get('info') %}
    {{ include ('base/__snackItem.html.twig', {snackClass: 'info'}) }}
{% endfor %}

{% for flashMessage in app.session.flashbag.get('error') %}
    {{ include ('base/__snackItem.html.twig', {snackClass: 'error'}) }}
{% endfor %}
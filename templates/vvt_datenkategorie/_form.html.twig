{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <fieldset>
        {{ form_row(form.name) }}
        {{ form_row(form.datenarten) }}
    </fieldset>
    {{ form_row(form.save, {row_attr: {class: 'form-buttons'}}) }}
    {{ form_rest(form) }}
{{ form_end(form) }}